<Page x:Class="ErogeHelper.View.Pages.LogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ErogeHelper.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LogPage">

    <Grid Margin="20, 10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <TextBox
            Grid.Row="0" Text="{Binding LogText, Converter={StaticResource QueueToTextConverter}}"
            VerticalContentAlignment="Top" FontFamily="Courier New" IsReadOnly="True" 
            HorizontalAlignment="Left"
            HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" />
        <TextBlock Grid.Row="1" Text="{Binding MaxLine}" Margin="0 10 0 0" />
    </Grid>
</Page>
