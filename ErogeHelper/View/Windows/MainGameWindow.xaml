<reactiveUi:ReactiveWindow
    x:Class="ErogeHelper.View.Windows.MainGameWindow"
    x:TypeArguments="vm:MainGameViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net" xmlns:vm="clr-namespace:ErogeHelper.ViewModel.Windows"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:MainGameViewModel}"
    Left="-32000" Top="-32000" Width="0" Height="0"
    Title="MainGameWindow"
    AllowsTransparency="True" ShowInTaskbar="False" Topmost="True" WindowStyle="None">

    <Window.Background>
        <SolidColorBrush />
    </Window.Background>

    <!--  Layout of the whole game window  -->
    <Border>
        <Grid>
            <Border x:Name="PreventFalseTouchMask"
                BorderBrush="Black" BorderThickness="5"
                Opacity="{StaticResource MinimalOpacity}" />
            <!--  Client area  -->
            <Border x:Name="ClientArea">
                <reactiveUi:ViewModelViewHost x:Name="AssistiveTouchHost" />
            </Border>
        </Grid>
    </Border>
</reactiveUi:ReactiveWindow>
