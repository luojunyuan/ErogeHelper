<rxui:ReactiveWindow
    x:Class="ErogeHelper.View.Windows.MainGameWindow"
    x:TypeArguments="vm:MainGameViewModel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rxui="http://reactiveui.net"
    xmlns:vm="clr-namespace:ErogeHelper.ViewModel.Windows;assembly=ErogeHelper.ViewModel"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:MainGameViewModel}"
    Left="-32000" Top="-32000" Width="0" Height="0"
    Title="MainGameWindow"
    AllowsTransparency="True" IsTabStop="False" ShowActivated="False"
    ShowInTaskbar="False" TabIndex="0" Topmost="True" WindowStyle="None">

    <Window.Background>
        <SolidColorBrush />
    </Window.Background>

    <!--  Layout of the whole game window or client area  -->
    <Border>
        <Grid>
            <Border x:Name="PreventFalseTouchMask"
                BorderBrush="Black" BorderThickness="5"
                Opacity="{StaticResource MinimalOpacity}" />

            <rxui:ViewModelViewHost x:Name="TouchToolBoxHost" Margin="5,50,0,0" />

            <!--  Client area  -->
            <Border>
                <rxui:ViewModelViewHost x:Name="AssistiveTouchHost" />
            </Border>
        </Grid>
    </Border>
</rxui:ReactiveWindow>
